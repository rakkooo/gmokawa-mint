{
  "name": "monad-mint-site",
  "private": true,
  "version": "1.0.0",
  "description": "GitHub Pages dApp for Kuru swap + NFT mint",
  "scripts": {
    "build": "esbuild src/app.js --bundle --format=iife --global-name=App --packages=bundle --minify --outfile=docs/app.bundle.js"
  },
  "dependencies": {
    "@kuru-labs/kuru-sdk": "^0.0.47",
    "ethers": "^5.7.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.4"
  },
  "engines": { "node": ">=20" }
}
